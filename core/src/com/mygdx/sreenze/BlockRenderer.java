package com.mygdx.sreenze;

import com.badlogic.gdx.graphics.Texture;
import com.badlogic.gdx.graphics.g2d.Batch;
import com.badlogic.gdx.scenes.scene2d.ui.Image;

public class BlockRenderer extends Image {
    /*
    private int blockSize;
    private int boardRows;
    private int boardCols;
    APiece piece;
    public BlockRenderer(APiece piece, String path, int blockSize, int boardRows, int boardCols) {
        super(new Texture(path));
        this.boardCols = boardCols;
        this.boardRows = boardRows;
        this.blockSize = blockSize;
    }
    public void update() {
        int x = blockSize * (boardCols - piece.getLocation().getCol());
        int y = blockSize * (boardRows - piece.getLocation().getRow());
        this.setPosition(x, y);
    }
*/
}
